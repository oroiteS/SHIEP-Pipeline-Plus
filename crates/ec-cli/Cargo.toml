[package]
name = "ec-cli"
version = "0.1.0"
edition = "2024"
autobins = false

[[bin]]
name = "SHIEP-Pipeline"
path = "src/main.rs"

[dependencies]
clap = { version = "4.5.60", features = ["derive"] }
ec-core = { version = "0.1.0", path = "../ec-core" }
